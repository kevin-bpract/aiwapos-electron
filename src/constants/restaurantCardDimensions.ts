/** Slider bounds for restaurant product cards (settings modal + clamp on save/load). */
export const RESTAURANT_CARD = {
  W_MIN: 120,
  W_MAX: 400,
  H_MIN: 160,
  H_MAX: 450,
  STEP: 10,
} as const;
